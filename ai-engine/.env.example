# AI Engine Environment Variables
# Copy this file to .env and fill in the values

# Secret API key used to protect the /bootstrap endpoint.
# Only the Node.js backend should know this key.
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
AI_API_KEY=replace-with-a-long-random-secret-key
